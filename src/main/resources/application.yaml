spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camdb
    username: root
    password: root
server:
  port: 9090

camunda.bpm:
  admin-user:
    id: demo
    password: demo
  filter:
    create: All tasks